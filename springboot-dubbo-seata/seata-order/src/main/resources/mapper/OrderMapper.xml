<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mscloudmesh.order.mapper.OrderMapper">

    <!--创建订单-->
    <insert id="createOrder" parameterType="com.mscloudmesh.order.entity.Order">
      insert into t_order values(null,#{orderNo},#{userId},#{commodityCode},${count},${amount})
    </insert>

</mapper>
